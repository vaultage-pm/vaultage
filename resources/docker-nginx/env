
export MYSQL_ROOT_PASSWORD=changeme

# HTTP port exposing the API and the client
export VAULTAGE_PORT=8080

# Change this to the absolute path to your copy of the vaultage repository
export VAULTAGE_DIR=`pwd`/../..

# Where to store mysql data. In production you'll want to use a location OUTSIDE the repository
# for instance:
# export MYSQL_DATA_DIR=/var/local/vaultage/mysql
export MYSQL_DATA_DIR=./mysql/data